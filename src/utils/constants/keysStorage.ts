export type KEYS_STORAGE = "language"

/**
 * A constant representing the key used to store the language preference in local storage.
 * This key is used to retrieve or save the user's selected language setting.
 */
export const LANGUAGE_KEY_STORAGE = "_languageKeyStorage";
